# Dependencies (reinstalled inside Docker)
node_modules
npm-debug.log*

# Next.js build output
.next
out

# Environment files — never bake secrets into image
.env
.env.*
!.env.example

# Dev / editor files
.git
.gitignore
.DS_Store
*.pem
*.tsbuildinfo

# Test / coverage
coverage
__tests__

# Docker files themselves
Dockerfile*
docker-compose*
# NOTE: docker-entrypoint*.sh must NOT be ignored — they are COPY'd into the image

# Docs
README.md
AGENTS.md
CLAUDE.md